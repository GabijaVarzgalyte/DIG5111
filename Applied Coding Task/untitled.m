%Write a MATLAB script/function which will allow the user to compare the frequency content of two audio files. 

%audioread() %% to read audio files
%audiowrite() %% to save processed audio
%normalize() %% normalising integers between -1 and 1
%fft() %% fast fourier transform for freq analysis
%plot() %%plotting graphs function
%conv() %%to apply convolution
%filter() %%applying a digital filter
